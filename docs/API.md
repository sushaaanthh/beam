# API

## Current Endpoints

- `GET /api/v1/health`: Basic health check for the FastAPI service.

## Planned Surface

- Emotion analysis and inference endpoints.
- Model metadata and explanation endpoints.
- Dataset and job orchestration endpoints.

The API surface is intentionally small right now while the repository is being restructured around the new B.E.A.M. architecture.